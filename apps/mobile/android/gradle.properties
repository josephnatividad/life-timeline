org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError --enable-native-access=ALL-UNNAMED
android.useAndroidX=true
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# Kotlin incremental caches cannot safely relativize plugin sources from the
# Pub cache on C: against this workspace on D:. Prefer deterministic native
# plugin compilation over an incremental build that silently drops classes.
kotlin.incremental=false
kotlin.compiler.execution.strategy=in-process
