/// Central semantic elevation roles for Quiet Intelligence surfaces.
///
/// These deliberately use restrained Material elevation. Exact shadow recipes
/// remain a visual-review item in the product design documentation.
abstract final class AppElevation {
  static const flat = 0.0;
  static const raised = 1.0;
  static const overlay = 3.0;
}
